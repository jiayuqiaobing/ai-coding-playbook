# 决策树：AI 编程的岔路口与已知陷阱

> **为什么用这种组织形式。**
>
> 普通清单是"我踩过什么坑"，按时间排列 —— 看完记不住，AI 也不会主动去查。
> 这份按 **决策节点** 组织：你在做某个动作**之前**，查一下对应节点，
> 就能看到历史上在这个节点翻过什么车、为什么翻、怎么避。
>
> **代价是真实的**：后知后觉找到原因，浪费的 token 和时间已经回不来了。
> 所以这份文档的目标不是"记录"，是"**事前可查**"。

---

## 0. 怎么用这棵树

### 0.1 快速索引：我现在在哪个节点？

| 你正在做的事 | 节点 |
|-------------|------|
| ⚠️ **准备等一个操作完成** | **N0 等待与卡死** ← **最高优先级** |
| 准备给 agent 派一个任务 | **N1 派活** |
| 准备跑一条可能很久的命令 | **N2 长跑任务** |
| 准备读取或解析一个文件 | **N3 文件解析** |
| 准备写文件、复制文件、改配置 | **N4 写入** |
| 准备 `git commit` 或 `git push` | **N5 提交推送** |
| 某个网络操作反复超时，但另一个工具却通 | **N5-3 代理不一致** |
| 准备装依赖或配环境 | **N6 环境依赖** |
| 准备启动子进程，或跨 shell 传变量 | **N7 进程与变量** |
| 准备写异常处理，或读安全开关 | **N8 错误与安全** |
| 准备改一个已知能跑的东西 | **N9 改动与验证** |
| 准备做对比实验或测量指标 | **N10 测量与实验** |
| 准备写一段"禁止 XX"的规则 | **N11 立规矩** |
| 看到报错，准备开始排查 | **N12 排错** |

### 0.2 记录标准：什么该记，为什么要记

**不是所有错误都值得记。** 记流水账会让这棵树变成没人读的坟场
（见 N11-1：规则文件过长会降低效果）。

**满足以下任意一条，就该记：**

| 判据 | 说明 |
|------|------|
| **① 浪费了不可恢复的资源** | token、时间、金钱 —— 花出去就回不来了 |
| **② 会重复发生** | 有明确的触发条件，下次同样的动作还会踩 |
| **③ 有可识别的触发点** | 能定位到"我在做什么的时候踩的"（= 能挂到某个决策节点上） |
| **④ 提前知道就能避开** | 如果动手前查过，这条错误根本不会发生 |

**四条都满足 → 必须记。** 只是"运气不好"或"一次性环境问题" → 不记。

**记录时的核心动机（为什么值得费这个事）：**

> **后知后觉找到原因，浪费的 token 和时间已经回不来了。**
>
> 记录的目的不是"留下历史"，是**让下一次不必重来** ——
> 让下一个 agent 在**动手之前**就能查到它。

**所以每条必须写清"症状 → 根因 → 修复"，并标上【代价】** ——
有代价的教训最难忘，也最能说服后来者别重蹈覆辙。

---

### 0.2.1 ⭐ 主动记录的义务

**满足上面四条判据的错误，不要等人提醒，应当当场记录并推送。**

这是**常设规则**，不依赖任何人开口：

```
1. 踩到满足判据的错误
   ↓
2. 立刻判断它属于哪个决策节点（N0~N12）
   ↓
3. 按 0.2 的格式追加到该节点（症状 / 根因 / 修复 / 代价）
   ↓
4. commit + push
   ↓
5. 如果本次暴露了新的"动手前自查"问题，同步更新 0.3
```

**判断"该不该记"时，问自己四个问题**（对应上面四条判据）：

1. 这次浪费了什么？（token / 时间 / 钱 / 一次机会）
2. 同样的动作再做一次，还会踩吗？
3. 我是站在哪个决策点上踩的？
4. **如果动手前查过这份树，我会不会避开？**

**第 4 条是最强的判据** —— 如果答案是"会"，那它**必须进树**。

> **不记的代价是隐性的**：你会以为"这次运气不好"，
> 然后在下一次换个壳子，踩同一个坑。

### 0.3 ⭐ 动手前的自查（最重要的用法）

**光"出事能查"不够 —— 要在动手前拦下来。**

**规则**：识别出自己处于哪个节点后，**先跑一遍该节点的自查清单**，
全过了再动手。清单很短，但每一条都对应一次真实事故。

| 节点 | 动手前问自己 | 不过就 |
|------|-------------|--------|
| **N0 等待** | **我这次等待有上限吗？有存活探针（进程/日志/产物）吗？没有的话，我怎么知道它是在跑、卡住了、还是已经死了？** | 加超时 + 探针 |
| **N1 派活** | 提示词里有没有"先去读 XX 文档"这类调研动作？能不能把信息**直接写进去**？写完 `Do nothing else` 了吗？ | 改短再发 |
| **N2 长跑** | 输出是重定向到文件，还是接了管道？加 `-u` 了吗？中途挂了会丢多少？ | 改重定向 + 增量落盘 |
| **N3 解析** | 这个文件我能**确定**是 UTF-8 吗？大小对吗（正常值的 2 倍 = UTF-16）？ | 先验编码再信内容 |
| **N4 写入** | **内容里有反引号或 `$` 吗？有未配对的引号吗？** 我是用 shell heredoc 写的吗？ | **换文件工具写**，不要用 shell |
| | `.gitignore` 改完后，我用 `git check-ignore -v` 验过吗？ | 验完再提交 |
| **N5 推送** | 我在哪个分支？默认分支在哪？`git rev-list --count origin/master..HEAD` 是多少？**网络操作超时了吗？别的工具却通？** | 先合并再推 / 查代理 |
| **N6 环境** | 是 `-r requirements.txt` 装的还是裸装包名？这个路径**现在**还存在吗？容器名/端口会不会撞车？ | 按约束装 + 验路径 |
| **N7 进程** | 子进程启动命令里是 `python` 还是 `sys.executable`？变量跨 shell 了吗？ | 改 `sys.executable` / 用文件引用 |
| **N8 错误** | 我写的异常处理会**留下痕迹**吗？安全开关没配置时的默认行为是放行还是拒绝？ | 加日志 / 改默认值为拒绝 |
| **N9 验证** | 我的断言覆盖**结构**了吗，还是只数了个数？"不传"和"传 None"语义一样吗？ | 补结构断言 / 用哨兵值 |
| **N10 测量** | 干预**真的发生了**吗（有没有证据）？我准备看均值还是也看波动？结论是靠数据还是靠感觉？ | 先证明干预生效 |
| **N11 立规矩** | 这条规则**能机械检查**吗？只写"禁止"而没给判断方法吗？ | 补判断方法或检查脚本 |
| **N12 排错** | 我看到的是**输出**还是**痕迹**？报错的"形状"是哪一类（全挂 vs 部分挂）？ | 看进程/日志/埋点，不看输出 |

**一句话版本**：
> **在容易出事的节点，先拿这棵树自查自己的内容，再行动。**

### 0.4 一条元规律

> **绝大多数"为了省事跳过某一步"的决定，都是未来某个故障的种子。**
>
> 这棵树里的陷阱，追溯到底几乎都是同一句话：**"当时觉得这一步没必要做。"**
>
> 而 0.3 那节自查清单，就是把这些"当时觉得没必要"的步骤，变成动手前的强制动作。

---

## N1 · 派活给 agent

| # | 陷阱 | 一句话 |
|---|------|--------|
| N1-1 | 提示词太长导致卡死 | 让它"先读一堆文档再动手" → 卡在 init 11 分钟零产出 |
| N1-2 | agent 会顺手多做 | 没写 `Do nothing else` → 它超出范围行事 |
| N1-3 | 忘记全局规则也约束它 | 你全局配置里的门槛规则会拦住它 |
| N1-4 | 把机械活交给贵模型 | 成本差 40~100 倍 |

### N1-1 提示词太长导致卡死

- **症状**：给 opencode 一条"先读 A 文档 → 再读 B 规则 → 再读 C 清单 →
  再搞懂跨仓库取文件的命令 → 才开始干活"的提示词。
  **卡死在 init 阶段 11 分钟，零产出、零文件改动、日志不增长。**
  同环境同模型，换成 80 词的短提示词（把完整命令直接写进去 + `Do nothing else`），
  **36 秒完成**。
- **根因**：每一步"去读个文件"都是一次模型往返。
  让它先自我调研，等于把 N 次往返花在准备工作上。
- **修复**：
  - **要它知道的信息，直接写进提示词**，不要让它自己去翻
  - 提示词尽量短，命令尽量完整
  - 明确写 `Do nothing else`
- **代价**：11 分钟 + 期间全部 token，产出为零

### N1-2 / N1-3 / N1-4

- **N1-2**：agent 会"顺手"多做（实测它自行补写了一个验证脚本）。
  不想让它做就明说。反过来，**想让它做的规矩要写进规则文件**。
- **N1-3**：agent 会读全局配置文件（如 `~/.claude/CLAUDE.md`），
  **你在全局定义的所有规则同样约束它**。派活前先想会不会被自己的规则拦住。
- **N1-4**：详见 N10-4。

---

## N0 · 等待与卡死 ⚠️ 最高优先级

> **这一类是所有故障里最不可容忍的。**
>
> 别的问题会报错、会留下线索；**卡死什么都不说** ——
> 它安静地烧掉你的时间，而你可能要等很久才发现。
>
> **卡死的本质不是"慢"，是"没有任何进度信号，你无法判断它到底在干什么"。**

| # | 陷阱 | 一句话 |
|---|------|--------|
| N0-1 | 无进度信号地等待 | 跑了 11 分钟零输出，你不知道它在想还是在死 |
| N0-2 | 等待时没有上限 | 干等 15 分钟，无法判断进程是活着还是已经没了 |
| N0-3 | 没有存活探针 | 不查进程/日志/产物，凭"感觉它在跑"判断 |
| N0-4 | 把"没输出"当成"没在跑" | 反向误判 —— 见 N12-1 |

### N0-1 无进度信号地等待（本次最严重的一次）

- **症状**：给 agent 派活后，**卡在初始化阶段 11 分钟**，
  零输出、零文件改动、日志不增长。你完全不知道它在干什么。
- **根因**：**每一步"去读个文件"都是一次模型往返**（见 N1-1）。
  让它先自我调研，等于把 N 次往返全花在准备工作上，
  而**这些往返之间没有任何可见输出**。
- **修复**：
  1. **派活前就把信息写进提示词**，消除"准备阶段的多次往返"（治本，见 N1-1）
  2. **给每一次等待设置上限**，超时则停掉并诊断
  3. **开日志**：很多工具支持 `--print-logs --log-level DEBUG`，
     有日志才谈得上判断
- **代价**：11 分钟；**更贵的是——你无法判断它到底是在思考还是已经死了，只能干等**

### N0-2 等待时没有上限

- **症状**：等待一个可能耗时的操作，用固定时长硬等，期间无法判断进程状态。
- **修复**：**任何等待都要有上限**，并以"轮询 + 存活探针"代替"整段硬等"：
  ```bash
  # 反例：整段硬等，期间完全不知道发生了什么
  sleep 900

  # 正例：分段轮询 + 每段都看进展
  for i in $(seq 1 30); do
      sleep 30
      printf "%ds: " $((i*30))
      wc -l < /tmp/task.log            # 日志在涨 = 活着在跑
  done
  ```
- **代价**：15 分钟无法判断进程死活

### N0-3 没有存活探针

**判断"还在跑吗"，永远用这三个探针，不要靠感觉：**

| 探针 | 命令 | 含义 |
|------|------|------|
| **进程在不在** | `tasklist \| grep xxx` / `ps aux \| grep xxx` | 进程没了 = 已经死了 |
| **日志涨不涨** | `wc -l < log`，隔 30 秒再看一次 | 数字在涨 = 在推进 |
| **产物变没变** | 检查输出文件的时间戳 | 有产物 = 至少推进到了那一步 |

**"活着但卡住" vs "已经死了" vs "只是慢"，三种状态必须分开判断** ——
它们的处理方式完全不同：

| 状态 | 特征 | 怎么办 |
|------|------|--------|
| **正在推进** | 日志在涨 / 产物在更新 | 继续等，别打扰 |
| **活着但卡住** | 进程在，日志不涨 | 停掉，查根因（可能在等输入） |
| **已经死了** | 进程没了 | 看退出码和最后的日志，按 N12 排错 |

### N0-4 反向误判

**"卡住了"和"跑完了但你没看到输出"是两种完全相反的情况**，
用同一套探针就能分开 —— 见 N12-1。

> 一次真实事故：一次 agent 驱动的评估"没有返回结果"，
> 我判定为失败，**实际它跑了 24 次调用然后正常结束了** ——
> 查埋点才发现。如果当时先跑一遍探针（进程没了 + 有产物文件），根本不会误判。

---

## N2 · 长跑任务

| # | 陷阱 | 一句话 |
|---|------|--------|
| N2-1 | 用管道接输出 → 进程被写死 | `\| tail` 读够就关管道 → 进程被杀，**退出码还是 0** |
| N2-2 | 结果攒到最后才写 | 进程中途死 → **全部丢失** |

### N2-1 管道写死进程（本次代价最大的一次）

- **症状**：`python -m eval.run | tail -30` 跑了 30 分钟，
  跑到 **20/40** 时进程消失，**而且没写结果文件**。**退出码居然是 0。**
- **根因**：`tail` 只保留最后 N 行，**读够就关闭管道**。
  之后进程再往 stdout 写就收到 **SIGPIPE**，被杀死。
  退出码 0 是因为进程死在管道链路里，异常没能传播出来。
- **修复**：
  ```bash
  # 写文件，不接管道；-u 关掉 Python 输出缓冲，能看到实时进度
  nohup python -u -m eval.run > /tmp/eval.log 2>&1 &

  # 想看进度另开一个 tail，不影响那个进程
  tail -f /tmp/eval.log
  ```
- **代价**：**20 个用例白跑**（约 15 分钟），结果全丢需要重跑

### N2-2 结果要增量落盘

- **修复**（N2-1 事故的结构性修复）：
  ```python
  def _flush(results, done):
      tmp.write_text(json.dumps(payload))   # 先写临时文件
      tmp.replace(path)                      # 再原子替换，避免读到半个文件
  # 每跑完一个单元就调一次，不要最后才调
  ```
- **判断标准**：**这个进程现在被 kill -9，我会损失多少？**
  答案是"全部"就说明设计有问题。

---

## N3 · 文件解析

| # | 陷阱 | 一句话 |
|---|------|--------|
| N3-1 | 编码损坏 | 文件"存在且大小正常"，但解释器根本读不了 |
| N3-2 | 只查存在性不查可用性 | 大小"看着对"就以为没问题 |

### N3-1 PowerShell 重定向写出 UTF-16

- **症状**：用 `>` 复制源码，文件存在、大小也"对"，
  但 Python 报 `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xff`。
- **根因**：**PowerShell 的 `>` 默认输出 UTF-16LE（带 BOM）**，不是 UTF-8。
  同一个文件：UTF-8 是 663 字节，PowerShell 写出来 **1270 字节（约 2 倍）**。
- **修复**：
  - 复制源码优先用 **Git Bash** 的重定向（默认 UTF-8）
  - **文件大小约为正常值 2 倍 = UTF-16 的信号**
- **代价**：坏文件已提交，事后才发现，需要修复并改写提交

### N3-2 验证要验"能用"，不是"存在"

- **修复**：任何写入或复制的源码文件，**用解释器解析一次**：
  ```bash
  python -c "import ast,pathlib; ast.parse(pathlib.Path('x.py').read_text(encoding='utf-8'))"
  ```
- **通用原则**：**"看起来成功"是最贵的失败形态。** 见 N9。

---

## N4 · 写入

| # | 陷阱 | 一句话 |
|---|------|--------|
| N4-1 | `.gitignore` 行尾注释 → 整条规则失效 | 注释被当成模式本身 |
| N4-2 | `.gitignore` 漏掉嵌套层级 | 只写 `chroma_db/`，实际路径是 `a/b/chroma_db/` |
| N4-3 | 排除目录后想放行目录内文件 | 排除目录 + 放行目录内文件 —— **无效** |
| N4-4 | 双引号里的反引号被 bash 执行 | 提交信息被替换成一堆 `command not found` |
| N4-5 | 同一个配置有两份来源 | 改了一份，部署读的是另一份 → 静默失效 |

### N4-1 / N4-2 / N4-3：gitignore 的三个坑

| 现象 | 错误写法 | 正确写法 |
|------|---------|---------|
| 规则完全不生效 | `logs/` 后面跟行内注释 | 注释**独占一行** |
| 嵌套目录没被忽略 | 只写顶层路径 | 用裸名（匹配所有层级） |
| 放行规则无效 | 排除目录 + 放行其中文件 | **先放行目录 → 再排除内容 → 最后放行文件** |

- **修复**：**写完必须验证，不要靠眼睛**：
  ```bash
  git check-ignore -v <文件>     # 有输出 = 被忽略
  ```
- **代价**：向量数据库（含真实对话数据）差点被推到公开仓库

### N4-4 用 shell 写含特殊字符的文本（反引号 / `$` / 引号）

**这是本树里唯一一条"记了还会再犯"的陷阱 —— 因为它有迷惑性。**

- **症状 A**：提交信息里写了带反引号的代码片段 →
  反引号被 shell 当成**命令替换**执行 →
  提交信息被替换成一堆 `command not found`，正文被破坏。

- **第一反应修复**：改用 heredoc，且分隔符**加引号禁止变量展开**：
  ```bash
  git commit -F - <<'MSGEOF'
  ...这里可以自由使用反引号和 $变量...
  MSGEOF
  ```

- **症状 B（更隐蔽）**：换成 heredoc 之后**又失败了** ——
  正文里含有**未配对的引号**（比如中文引号与英文单引号混排、
  或正文里写的示例代码本身就带引号），shell 照样报
  `unexpected EOF while looking for matching`，
  **整个命令没执行，文件也没写成**。

- **根因**：heredoc 只解决了"变量展开"，**没有解决"引号配对"** ——
  shell 仍然要先行解析整段内容。

- **修复（最终做法）**：
  - **凡是要写长文本（>20 行）或含特殊字符的内容，一律用文件工具/编辑器写，不要用 shell**
  - 非要用 shell 时：**先在本地文件里写好，再 `git commit -F 文件` / `curl -d @文件`**
  - 判断信号：**内容里只要出现反引号、`$`、引号，就该警惕**

- **代价**：连续两次失败，两次都要重新组织命令。
  **而这两次失败本可以在动手前用 0.3 的自查清单拦下。**

> **教训不在于"该用 heredoc 还是引号"，而在于**：
> **知道有坑 ≠ 不会踩。把检查变成动手前的固定动作，才是解法。**

### N4-5 单一配置来源

- **症状**：安全开关加进了**子目录**的配置模板，但没加进**根目录**的；
  而部署读的是根目录的 → **鉴权静默失效**。
- **修复**：**发现第二份配置来源就删掉它**，不要"两边都更新"。

---

## N5 · 提交推送

| # | 陷阱 | 一句话 |
|---|------|--------|
| N5-1 | 工作分支 ≠ 默认分支 | 一直在 A 分支干活，推送的却是落后的 B |
| N5-2 | 长期不推 | 两天的工作全在本地，远端停在最初提交 |

### N5-1 推错分支

- **症状**：本地 HEAD 是 `c026deb`，推送后远端只有 `a1b45c3`，还以为推送失败。
- **根因**：一直工作在 `stage1` 分支，而默认分支停在很早的位置。
  **推送命令推的永远是你当前检出的分支，不是"最新的工作"。**
- **修复**：推送前先确认：
  ```bash
  git status -sb                              # 看当前分支与领先/落后
  git rev-list --count origin/master..HEAD    # 看有多少提交没推
  ```
  工作分支完成后快进合并到默认分支：
  ```bash
  git checkout master && git merge stage1 --ff-only && git push origin master
  ```
- **代价**：以为推成功了，实际推的是半成品，需要二次修正

### N5-2 定期推送

- **症状**：32 个提交、3500 行代码全在本地，远端还停在骨架阶段。
- **修复**：**把"推送"写进阶段完成的定义**，它不是可选项。
  每个阶段结束、或每天结束，至少推一次。

### N5-3 网络操作超时，但**另一个工具却通** —— 代理不一致

- **症状**：`git push` 连续失败：
  ```
  Failed to connect to github.com port 443 after 21129 ms: Could not connect to server
  Connection was reset
  ```
  **但同一时刻 `gh api` 完全正常**，能读到远端文件。
  反复重试 3 次，每次都等 21 秒超时。

- **根因**：**不同工具读代理配置的来源不同**：
  - `gh`（Go 程序）**自己读系统/环境代理**
  - `git` **默认不读系统代理**，除非显式配置 `http.proxy`
  - 实测环境中代理（Clash 类）监听着 `127.0.0.1:7897`，
    但 `git config --get http.proxy` 是空的 → git 直连 → 被墙

- **修复**（临时，只对本次命令生效，不污染全局）：
  ```bash
  git -c http.proxy=http://127.0.0.1:7897 \
      -c https.proxy=http://127.0.0.1:7897 push origin master
  ```
  需要长期生效再写进 `git config --global`。

- **诊断法（重要）**：
  > **"工具 A 失败、工具 B 成功" 时，先怀疑它们读的配置来源不同，
  > 不要怀疑网络本身。**
  先查：`git config --get http.proxy`、`env | grep -i proxy`、
  代理端口是否在监听（`netstat -an | grep <端口>`）。

- **代价**：3 次 × 21 秒 = 63 秒，加上一次失败的推送和一轮排查

- **同族陷阱**（同一个根因，不同表现）：
  - 环境变量**不跨 shell**（见 N7-2）
  - 子进程用裸 `python` 解析到**另一个解释器**（见 N7-1）
  - 容器内 `localhost` 指向**自己**而不是另一个容器（见 N6-4）

  > **共同模式：你以为是"环境 B 坏了"，其实是"工具 A 用错了配置源"。**
  > 遇到这类问题，**先确认每个工具各自的配置来源**，再下结论。

---

## N6 · 环境依赖

| # | 陷阱 | 一句话 |
|---|------|--------|
| N6-1 | 裸装包名而非按约束装 | 装了 2.x，代码用 1.x API → 全崩 |
| N6-2 | 声明版本与实际安装漂移 | 声明 0.34，实际 0.46，没人知道 |
| N6-3 | 记录的环境路径会过时 | 文档说用 A 环境，三周后 A 已被删了一半 |
| N6-4 | 容器名/端口是全局资源 | 与另一个正在跑的项目冲突 |
| N6-5 | 拉基础镜像失败常是镜像源的问题 | 403 来自第三方加速器，不是网络 |

### N6-1 按约束装包

- **修复**：
  ```bash
  python -m pip install -r requirements.txt    # 不要裸装包名
  ```
- **通用**：遇到 `ModuleNotFoundError`，**先怀疑版本不对**，不只是"没装"。

### N6-2 验证声明与实现一致

- **修复**：逐条比对，不要靠记忆：
  ```python
  from packaging.requirements import Requirement
  from packaging.version import Version
  import importlib.metadata as md

  for line in open("requirements.txt"):
      req = Requirement(line.strip())
      ok = req.specifier.contains(Version(md.version(req.name)))
  ```

### N6-3 路径会过时

- **症状**：记录写着"用某环境，依赖最全"。三周后要用时发现：
  **该环境已被部分删除** —— 解释器、标准库、包记录全没了，
  只剩一堆**跑不了**的第三方包。
- **修复**：
  - **引用任何路径前，先验证它存在**
  - 判断 conda 环境健康度：`conda list -n <env>` 是否正常输出包列表
    —— **只输出表头 = 环境元数据已损坏**
  - **记忆和文档会过时，代码不会骗你**

### N6-4 命名加前缀

- **修复**：容器名和宿主端口都加项目/版本前缀，这样能与其他项目**并存**，
  不必停掉别人正在跑的服务。
- **排查**用 `docker ps -a`（含已停止的），不只用 `docker ps`。

### N6-5 镜像源写进 Dockerfile

- **修复**：不要写裸的 `FROM python:3.11-slim` 然后指望本机加速器可用，
  **直接把可用的源写进去**，换机器也不用重配。

---

## N7 · 进程与变量

| # | 陷阱 | 一句话 |
|---|------|--------|
| N7-1 | 子进程用裸 `python` | 解析到错误解释器，子功能全废 |
| N7-2 | 跨 shell 读不到环境变量 | 误判成密钥失效 |

### N7-1 用 `sys.executable`

- **症状**：服务能启动，但日志报 `No module named 'xxx'`，子功能一个都发现不了。
- **根因**：代码写死 `"command": "python"`，子进程解析到 **base 环境**的解释器，
  而依赖装在另一个环境里。
- **修复**：
  ```python
  "command": sys.executable,                                                    # 永远是"当前这个解释器"
  "args": [os.path.join(os.path.dirname(os.path.abspath(__file__)), "x.py")],   # 绝对路径
  ```
- **通用**：**多环境机器上，裸 `python` 指向哪个解释器是未知数。**

### N7-2 环境变量不跨 shell

- **症状**：PowerShell 里命令正常，切到 Git Bash 报密钥无效，
  **且所有模型都报同样错**。
- **根因**：环境变量用 PowerShell 语法设置，**Git Bash 读不到**。
- **诊断法**：**"所有模型同时报同一个错"是环境问题的特征**；
  上游挂了只会挂某几个模型。
- **修复**：
  - 统一用设置变量的那个 shell，**或**
  - **干脆绕开环境变量** —— 支持文件引用的工具就用文件引用，
    跨 shell 也不会失效

---

## N8 · 错误与安全

| # | 陷阱 | 一句话 |
|---|------|--------|
| N8-1 | `except: pass` 吞异常 | 整条功能链静默失效，几个月没人知道 |
| N8-2 | 安全开关默认"方便" | 未配置时放行 → 上线等于裸奔 |

### N8-1 永不静默吞异常

- **症状**：向量检索永远返回空，**但没有任何报错**，持续了整个项目周期。
- **根因**：底层调用失败返回错误码，而调用处写了 `except: pass`。
- **修复**：
  - **永远不用 `except: pass`**，至少记一条 warning 并带上堆栈
  - 任何"降级"路径都要**留可观测的痕迹**
  - 外部依赖首次接入，**先单独写脚本验证它能通**，再接进主流程

### N8-2 安全默认值必须是"安全"的

- **症状**：代码写的是"没配置密钥就放行（开发模式）"，
  配置模板漏了这个键 → 部署时为空 → **鉴权静默失效**。
- **修复**：**安全开关不配置时的默认行为是"拒绝"，不是"放行"**。
  要放行就显式配置。

---

## N9 · 改动与验证

| # | 陷阱 | 一句话 |
|---|------|--------|
| N9-1 | 只看数量不看结构 | "产生了 3 个 span"全对，但树结构是坏的 |
| N9-2 | 参数的 `None` 兼做两义 | 根节点把自己当成了父节点 |
| N9-3 | 依赖隐式继承而不是显式创建 | 上下文里存着根自己 → 自环 |

### N9-1~N9-3：一次真实的连环 bug

- **症状**：trace 树里根节点的 `parent_span_id` 指向它自己。
- **根因**：两个缺陷叠加
  1. 进入上下文时把"当前节点"设成了**根节点**，
     而创建节点时默认从上下文取父节点 → **自己当自己的爹**
  2. 更根本的：那个入口函数只返回 id 不返回对象本身，
     调用方**没法**把子节点挂到根上
- **修复**：
  - **凡是"不传"和"传 None"语义不同的参数，必须用哨兵值区分**
  - **树形结构的根节点要显式创建**，不要靠隐式继承
  - **断言要覆盖结构**：共享同一 id / 根无父 / 子指向根 ——
    只断言"产生了 3 个 span"的话，这个 bug 会一直藏着

---

## N10 · 测量与实验

| # | 陷阱 | 一句话 |
|---|------|--------|
| N10-1 | 没验证"两组真的有差异"就开始测 | 干预没发生，测的是噪声 |
| N10-2 | 只看均值不看波动 | 差值小于标准差 —— 毫无统计意义 |
| N10-3 | 指标被自己的测试数据污染 | 把验证脚本的失败当成生产问题去查 |
| N10-4 | 凭感觉估成本 | 估 ¥5~15，实际 ¥0.21 —— **错 40 倍** |

### N10-1 先验证干预发生了

- **症状**：做 A/B 对照验证某布局的影响，跑完发现 B 比 A 低 23%，
  正准备下结论 —— **查记录才发现 9 次调用里干预只生效了 1 次**。
  **两种条件下的实际状态在 8/9 的情况下完全一样。根本没测到想测的东西。**
- **修复**：跑实验前**确认干预真的发生了**。
  加一条断言、打一行日志，比事后补数据便宜得多。

### N10-2 看波动范围

- **修复**：
  ```
  差值必须显著大于波动，才谈得上结论。
  差值 29% < 波动 32%  →  结论是"无法区分"，不是"B 更差"
  ```
- **能诚实说"测不出来"，比编一个结论有价值。**

### N10-3 先按来源过滤

- **修复**：指标里混了测试数据时，**先按来源过滤再下结论**。
- **本项目做法**：用 `session_id` 前缀区分
  （`eval-*` 评估用例 / `verify-*` 验证脚本 / `default` 手工测试）。

### N10-4 成本要查数据

- **症状**：估"跑 40 条评估约 ¥5~15"，实际 **¥0.21**。
- **根因**：把两种执行方式混为一谈

  | 方式 | 每次调用 token | 40 用例成本 |
  |------|---------------|------------|
  | 直接跑脚本 | ~1.5K | **¥0.21** |
  | 用 agent 驱动（它在中间读文件、推理、看输出） | 1 万~5 万 | ¥5~15 |

- **最讽刺的地方**：这个项目**本身就是做可观测的**，有埋点有指标，
  **而我却在凭感觉估成本**。**工具做出来了不等于会用它。**
- **修复**：**估算一律查埋点数据**，不要拍脑袋。

---

## N11 · 立规矩

| # | 陷阱 | 一句话 |
|---|------|--------|
| N11-1 | 规则写太长 | 超过 150 行收益递减，推理成本 +20~23% |
| N11-2 | 用 LLM 生成规则文件 | 实测**降低**任务成功率 |
| N11-3 | 只写"禁止 XX"不给判断方法 | agent 不知道该怎么自查 |
| N11-4 | "写进文档" ≠ "会遵守" | 自己写了"别用 emoji"，然后用了 emoji |
| N11-5 | 没给 agent 提问的出口 | 它想问也被拒 → 要么硬做决定要么直接失败 |

### N11-1 / N11-2 规则文件要短、要手写

- **研究结论**（2500+ 仓库统计）：规则文件超过 **150 行收益递减**，
  且**推理成本增加 20~23%**；某些工具会在 32 KiB 处**静默截断**。
- **更反直觉的一条**：**LLM 生成的规则文件会降低任务成功率**
  （8 个测试场景里 5 个变差，每任务多 2.45~3.92 步）。
- **修复**：控制在 150 行内，详细内容放子文档按需加载；**人手写**。

### N11-3 硬性禁止要给判断方法

```
❌ 禁止修改测试文件
✅ 禁止修改 agent-lite/tests/ 下的任何文件。
   判断方法：git diff --stat 里不得出现该目录。
   失败只能改实现，不能改断言、不能 skip。
```

### N11-4 把规则变成检查

- **症状**：项目踩坑库的第一条就是"控制台打不了 emoji，用 ASCII 标记"。
  **然后写新脚本时用了 emoji，程序在 GBK 控制台崩溃。**
- **根因**：文档是给"想起来的时候"用的，
  但人在专注写代码时**不会主动去翻**。
- **修复**：**能变成检查的，不要只写成提醒**：
  ```python
  bad = [ch for ch in src if ord(ch) > 0x1F000]   # 任何 emoji
  if bad: raise SystemExit("混进了 emoji，Windows 控制台会崩")
  ```

### N11-5 给 agent 一个写问题的出口

- **修复**：规定一个**文件形式的提问机制**：
  ```markdown
  ## [任务号] 一句话说明卡在哪
  - 背景 / 选项 A（优缺点）/ 选项 B（优缺点）/ 我的倾向 / 阻塞程度
  ```
  并规定：**可绕过的** → 跳过先做别的；**完全阻塞的** → 停下等人。
- **原则**：**写进文件不是回避，是承认信息不足。假装解决才是回避。**

---

## N12 · 排错

| # | 陷阱 | 一句话 |
|---|------|--------|
| N12-1 | "我没看到输出"就判定失败 | 它其实跑了，结果没回传 |
| N12-2 | 过早归结为环境问题或上游问题 | 报错的"形状"能告诉你是哪类 |

### N12-1 判断"跑没跑"要看痕迹

- **症状**：一次用 agent 驱动的评估，结果没回传，
  我判定为"失败"并告诉用户浪费了钱。
  **实际它跑了 24 次真实模型调用** —— 查埋点才发现的。
- **修复**：判断"有没有执行"，**看痕迹而不是看输出**：
  - 进程列表 / 进程存活时间
  - 日志文件的修改时间与行数增长
  - **埋点数据**
  - 产物文件（结果 JSON、生成的代码）

### N12-2 报错的"形状"会告诉你根因类别

| 报错形状 | 大概率是 |
|---------|---------|
| **所有**模型/所有接口都报同一个错 | **环境问题**（变量没传、凭据没读到） |
| 只有**某几个**模型报错 | 上游问题（那个通道挂了） |
| 报错 + 有产物 | 部分失败，看产物到哪一步 |
| 无报错 + 无产物 + 进程还在 | **卡住了**（等输入，或死锁） |

---

## 附 A：一族反复出现的"元模式"

**这棵树里有些陷阱看着毫不相干，但根因是同一个。** 把它们拎出来单列：

### 模式一：你以为是"环境坏了"，其实是"工具用错了配置源"

| 表现 | 真相 | 节点 |
|------|------|------|
| `git push` 超时，但 `gh api` 正常 | git 不读系统代理，gh 读 | N5-3 |
| PowerShell 里能跑，Git Bash 里报密钥无效 | 环境变量不跨 shell | N7-2 |
| 服务启动了，但子功能找不到模块 | 子进程用裸 `python` 解析到别的解释器 | N7-1 |
| 容器里连不上另一个容器 | `localhost` 指向容器自己 | N6-4 |

> **诊断法**：**"工具 A 失败、工具 B 成功" → 先查它们各自的配置来源**，
> 不要先怀疑网络或环境本身。

### 模式二：看起来成功了，实际是坏的

| 表现 | 真相 | 节点 |
|------|------|------|
| 文件存在、大小也对 | 编码是 UTF-16，解释器读不了 | N3-1 |
| 测试 100% 通过 | 测试被改过 / 被 skip 了 | N8-1 |
| 检索正常返回 | 永远返回空，异常被吞了 | N8-1 |
| 鉴权代码写了 | 密钥没配置 → 静默放行 | N8-2 |
| 评估跑完了 | 结果没落盘，进程其实死了 | N2-2 |

> **共同点**：**没有报错。**
> 判断标准必须是"**能不能用**"，不是"**看起来对不对**"。

### 模式三：没有进度信号的等待

| 表现 | 真相 | 节点 |
|------|------|------|
| 卡了 11 分钟零输出 | 每步都在做模型往返，无可见进度 | N0-1 |
| 干等 15 分钟不知道死活 | 没有存活探针 | N0-2 |
| 输出被管道吃掉 | 管道关闭导致进程被杀 | N2-1 |

> **处理**：任何等待都要有 **上限 + 存活探针**。

---

## 附 B：怎么扩展这棵树

**在节点下追加**：按 0.2 的格式写，并标上**代价** ——
有代价的教训最难忘，也最能说服后来者别重蹈覆辙。

**不属于任何现有节点** → **新建一个分支**，并在 0.1 的索引表里加一行。

**最重要的一点**：每次踩坑后先问自己 **"我当时站在哪个决策点"**，
再往那个节点里记 —— 这样下一个 agent 才有可能在**动手之前**查到它。

> 这棵树的最终目标不是"记录历史"，是让**下一次不需要重来**。
